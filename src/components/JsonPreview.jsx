export default function JsonPreview({ formData }) {
  return (
    <div className="card">

      <h2>Login Success 🎉</h2>

      {/* <pre>
        {JSON.stringify(formData, null, 2)}
      </pre> */}

    </div>
  );
}